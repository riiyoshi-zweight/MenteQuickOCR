SUPABASE_URL=
SUPABASE_ANON_KEY=
# 64 バイト以上のランダム値を推奨: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=
# SMTP 設定 (未設定の場合は開発用に console にリセット URL を出力)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="noreply@example.com"
NEXT_PUBLIC_APP_URL=http://localhost:3000
